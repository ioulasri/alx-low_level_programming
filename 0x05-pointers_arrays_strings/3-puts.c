#include "main.h"

/**
 * _puts - prints a given string
 * @s: the string
 * Return: the length of given string
 */

void _puts(char *str)
{
	int i;

	i = 0;
	while (s[i])
	{
		_putchar(s[i]);
		i++;
	}
}
