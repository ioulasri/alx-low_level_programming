#include "main.h"

/**
 * _islower - check for lowercase char.
 * @c: the char to be checked.
 * Return: 1 if given char x is lowercase, 0 otherwise.
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}

